# Copy this file to .env and fill in your own values (or just leave it out —
# the script will prompt you interactively instead, with the password hidden).
# .env is gitignored; never commit real credentials.

FB_EMAIL=
FB_PASSWORD=
